import { eqNum } from 'dummy/helpers/eq-num';
import { module, test } from 'qunit';

module('Unit | Helper | eq num');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = eqNum([42]);
  assert.ok(result);
});
